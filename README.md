# Transactional Order and Inventory System

A backend system that manages clients, orders, products, and inventory. It makes each order operation atomic and protects inventory consistency when multiple clients order concurrently.

**Current status:** Design phase